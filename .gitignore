# Python bytecode
__pycache__/
*.pyc
*.pyd
*.pyo
*.egg-info/
.pytest_cache/

# Virtual environment
# This should match the name of your virtual environment folder (often 'venv' or 'env')
venv/
env/

# Database
# If you were using SQLite, this would ignore the local database file
# You are using MySQL now, so this is less critical but good practice if you ever switch
db.sqlite3

# Environment variables / Secrets
.env
.flaskenv # If using Flask, but good to include general env files

# IDE specific files
.idea/           # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/         # VS Code settings (unless you want to share specific workspace settings)
*.iml            # IntelliJ IDEA project files
.project         # Eclipse project files
.pydevproject    # PyDev project files

# Operating System files
.DS_Store        # macOS
Thumbs.db        # Windows

# Static and Media files generated by Django
# These are usually built/collected during deployment, not source code
static_cdn/       # Or whatever your COLLECTSTATIC_ROOT is set to for collected static files
media/           # User-uploaded files
staticfiles/
media_uploads/

# Logs
*.log
*.log.*